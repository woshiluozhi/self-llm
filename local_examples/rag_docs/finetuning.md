# Finetuning Stage

微调阶段负责让模型更适合特定领域或特定风格。常见方法包括 LoRA、P-tuning 和全量微调。

LoRA 是参数高效微调方法，通常只训练少量低秩适配器参数，显存成本比全量微调低。P-tuning 会训练一组可学习的软提示词，让模型在不修改主体参数的情况下适配任务。全量微调会更新模型全部参数，成本最高，但可控性也更强。

微调后部署阶段需要加载基础模型和微调权重，运行同样的问题，对比微调前后的回答差异，再决定是否把微调后的模型接入 API 或 WebDemo。
